import { StyleSheet } from 'react-native';
import { colors } from 'shared/styles';

const styles = StyleSheet.create({
  root: {
    height: 1,
    backgroundColor: colors.alpha.gray36,
  },
});

export default styles;
