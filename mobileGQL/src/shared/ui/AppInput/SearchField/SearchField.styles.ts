import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  root: { zIndex: 1 },
  iconContainer: {
    position: 'absolute',
    top: 12,
    left: 16,
    width: 20,
    height: 20,
    zIndex: 1,
  },
  input: {
    paddingLeft: 46,
  },
});

export default styles;
