import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  root: {
    flexGrow: 1,
    flexDirection: 'row',
    alignItems: 'flex-end',
  },
  inner: {
    flex: 1,
    paddingBottom: 32,
  },
});
