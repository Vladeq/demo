import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tags: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  tag: {
    marginHorizontal: 5,
    marginVertical: 5,
  },
});

export default styles;
